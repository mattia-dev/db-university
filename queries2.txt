SELECT YEAR(enrolment_date), count(*) FROM `students` GROUP BY YEAR(enrolment_date);
SELECT office_address, COUNT(*) FROM `teachers` GROUP BY office_address;
SELECT exam_id, AVG(vote) FROM `exam_student` GROUP BY exam_id;
SELECT department_id, COUNT(department_id) FROM `degrees` GROUP BY department_id;
SELECT students.name, students.surname FROM `students` INNER JOIN degrees ON students.degree_id = degrees.id WHERE degrees.name = 'Corso di Laurea in Economia';
SELECT degrees.name FROM `degrees` INNER JOIN departments ON departments.id = degrees.department_id WHERE departments.name = 'Dipartimento di Neuroscienze';
SELECT courses.name FROM `course_teacher` INNER JOIN courses ON courses.id = course_teacher.course_id WHERE course_teacher.teacher_id = 44;
SELECT students.surname, students.name, degrees.name, departments.name FROM `students` LEFT JOIN degrees ON degrees.id = students.degree_id LEFT JOIN departments ON departments.id = degrees.department_id ORDER BY students.surname ASC, students.name ASC;
SELECT degrees.name, courses.name;
SELECT teachers.surname, teachers.name FROM `course_teacher` LEFT JOIN teachers ON teachers.id = course_teacher.teacher_id LEFT JOIN courses ON courses.id = course_teacher.course_id LEFT JOIN degrees ON degrees.id = courses.degree_id LEFT JOIN departments ON departments.id = degrees.department_id WHERE departments.id = 5 ORDER BY teachers.surname, teachers.name;
SELECT students.surname, students.name, courses.name, COUNT(exam_student.exam_id) FROM `students` INNER JOIN `exam_student` ON students.id = exam_student.student_id INNER JOIN `exams` ON exams.id = exam_student.exam_id INNER JOIN `courses` ON courses.id = exams.course_id GROUP BY exam_student.exam_id ORDER BY students.surname, students.name;